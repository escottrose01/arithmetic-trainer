package main.records;

// TODO: replace with modular problem types
/**
 * A simple enum representing a type of problem.
 */
public enum ProblemType {
    ADDITION,
    SUBTRACTION,
    MULTIPLICATION,
    DIVISION,
    COMBO
}
